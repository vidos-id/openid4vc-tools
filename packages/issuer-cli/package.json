{
	"name": "@vidos-id/issuer-cli",
	"description": "CLI for dc+sd-jwt credential issuance and issuer trust material.",
	"version": "0.7.0",
	"repository": {
		"type": "git",
		"url": "https://github.com/vidos-id/oid4vp-cli-utils.git"
	},
	"type": "module",
	"main": "./src/index.ts",
	"module": "./src/index.ts",
	"types": "./src/index.ts",
	"bin": {
		"issuer-cli": "./src/index.ts"
	},
	"publishConfig": {
		"registry": "https://npm.pkg.github.com",
		"access": "public"
	},
	"scripts": {
		"build": "bun build src/index.ts --target=bun --outdir=../../dist/issuer-cli --entry-naming=[name].js --define __CLI_VERSION__='\"'$npm_package_version'\"'",
		"check-types": "tsc --noEmit --project ../../tsconfig.json",
		"test": "bun test --pass-with-no-tests"
	},
	"dependencies": {
		"@vidos-id/cli-common": "workspace:*",
		"commander": "^14.0.3",
		"zod": "^4.3.6",
		"@vidos-id/issuer": "workspace:*"
	}
}
